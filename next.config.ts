// next.config.js
/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  basePath: '/hungiie.github.io',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;

